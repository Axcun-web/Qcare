export async function api(path, options = {}) { const token = localStorage.getItem("qcare_token"); const response = await fetch(`/api${path}`, { ...options, headers: { "Content-Type": "application/json", ...(token ? { Authorization: `Bearer ${token}` } : {}), ...options.headers } }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.message ?? "Permintaan gagal diproses"); return body; }
